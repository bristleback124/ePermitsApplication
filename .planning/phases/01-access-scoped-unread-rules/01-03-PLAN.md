---
phase: 01-access-scoped-unread-rules
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - Services/Interfaces/IChatService.cs
  - Services/ChatService.cs
  - Controllers/ChatController.cs
  - tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
must_haves:
  truths:
    - "Applicants only count reviewer-side unread on their own applications."
    - "Assigned department users only count applicant unread on applications where they are the assigned reviewer, while admins can count their own admin recipient rows for applications they can currently oversee."
    - "Mark-read clears only the current actor's opposite-side unread rows for one application and leaves other actors and other applications unchanged."
    - "Losing current access blocks unread visibility and clear actions even if old recipient rows still exist."
  artifacts:
    - path: "Services/ChatService.cs"
      provides: "Canonical unread scope rule for send, count, and clear"
      contains: "ResolveUnreadRecipientUserIdsAsync"
    - path: "Controllers/ChatController.cs"
      provides: "Stable unread-count and mark-read endpoints delegating sender-side decisions to the service"
      contains: "GetUnreadCount"
    - path: "tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs"
      provides: "Green Phase 1 verification suite"
      contains: "CHAT_03"
  key_links:
    - from: "Controllers/ChatController.cs"
      to: "Services/ChatService.cs"
      via: "GetUnreadCountAsync and MarkAsReadAsync without controller-side senderType branching"
      pattern: "GetUnreadCountAsync\\(applicationId, userId, userRole\\)|MarkAsReadAsync\\(applicationId, userId, userRole\\)"
    - from: "Services/ChatService.cs"
      to: "Repositories/MessageRepository.cs"
      via: "recipient-aware unread count, clear, and add-recipient operations"
      pattern: "AddRecipientStatesAsync|GetUnreadCountAsync|MarkAsReadAsync"
---

<objective>
Enforce the canonical Phase 1 unread rule in the chat service and keep the API surface explicit.

Purpose: Make unread visibility, recipient creation, and mark-read behavior follow one backend rule source that later phases can reuse.
Output: Updated chat service/controller contracts and green Phase 1 unread tests.
</objective>

<execution_context>
@C:/Users/Harvie/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Harvie/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
@.planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
@Controllers/ChatController.cs
@Services/Interfaces/IChatService.cs
@Services/ChatService.cs
@Repositories/Interfaces/IMessageRepository.cs
@Repositories/Interfaces/IApplicationRepository.cs
@Repositories/MessageRepository.cs
@Services/ApplicationService.cs
@Repositories/ApplicationRepository.cs
@tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs

<interfaces>
Current controller entrypoints:
```csharp
[HttpPut("{applicationId}/mark-read")]
public async Task<IActionResult> MarkAsRead(int applicationId)

[HttpGet("{applicationId}/unread-count")]
public async Task<IActionResult> GetUnreadCount(int applicationId)
```

Current service contract:
```csharp
Task MarkAsReadAsync(int applicationId, int userId, string userRole, string senderType);
Task<int> GetUnreadCountAsync(int applicationId, int userId, string userRole, string senderType);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the canonical unread scope and recipient snapshot logic in ChatService</name>
  <read_first>
    .planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
    .planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
    Services/ChatService.cs
    Services/Interfaces/IChatService.cs
    Services/ApplicationService.cs
    Repositories/Interfaces/IMessageRepository.cs
    Repositories/Interfaces/IApplicationRepository.cs
    Repositories/ApplicationRepository.cs
    Repositories/UserRepository.cs
    Entities/ApplicationDepartmentReview.cs
    tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs
  </read_first>
  <files>Services/Interfaces/IChatService.cs, Services/ChatService.cs</files>
  <behavior>
    - Test 1: Applicant count/read only target reviewer-side (`SenderType == "Government"`) recipient rows on owned applications.
    - Test 2: Reviewer-side count/read only target applicant (`SenderType == "Applicant"`) recipient rows when the current actor is either the assigned `user` reviewer or an `admin` with live access.
    - Test 3: Sending a message snapshots recipients at send time: applicant messages create recipient rows for assigned reviewers plus current admins per D-01, D-02, D-03, and D-06; reviewer/admin messages create recipient rows only for the owning applicant per D-05.
  </behavior>
  <action>Change `IChatService` so `MarkAsReadAsync` and `GetUnreadCountAsync` take exactly `(int applicationId, int userId, string userRole)` and move sender-side branching into `ChatService` per D-08. In `ChatService`, implement one canonical access/scope path using `GetUnreadScopeApplicationAsync`: applicants require `application.UserId == userId`; `user` role requires `application.DepartmentReviews.Any(r => r.AssignedReviewerId == userId)` per D-02; `admin` role is allowed under existing admin oversight access per D-03. Add private helpers with concrete names `ResolveUnreadRecipientUserIdsAsync`, `GetUnreadSenderTypeForActor`, and `ValidateUnreadAccessAsync`. On `SendMessageAsync`, persist the `Message`, then call `AddRecipientStatesAsync` with distinct recipient rows resolved as follows: applicant sender -> every non-null `AssignedReviewerId` on the application plus every current admin user id, each with `RecipientRole` copied from the target user role and `SenderType = "Applicant"`; admin/user sender -> one recipient row for `application.UserId` with `RecipientRole = "applicant"` and `SenderType = "Government"`. On `GetUnreadCountAsync` and `MarkAsReadAsync`, always re-run live access validation per D-07, derive the opposite sender type inside the service, and call the recipient-aware repository methods with `recipientUserId = userId`. Keep `GetMessagesAsync` side-effect free, but stop using the global `Message.IsRead` flag as the unread authority: map `MessageDto.IsRead` from the current actor's matching recipient row for opposite-side messages, and treat same-side sent messages as read for that actor.</action>
  <acceptance_criteria>
    `rg -n "Task MarkAsReadAsync\\(int applicationId, int userId, string userRole\\)|Task<int> GetUnreadCountAsync\\(int applicationId, int userId, string userRole\\)" Services/Interfaces/IChatService.cs Services/ChatService.cs` matches the new service signatures.
    `rg -n "ResolveUnreadRecipientUserIdsAsync|ValidateUnreadAccessAsync|GetUnreadSenderTypeForActor" Services/ChatService.cs` confirms the canonical helper seam exists.
    `rg -n "AssignedReviewerId == userId|UserRoleDesc|AddRecipientStatesAsync|RecipientRole|SenderType = \\\"Applicant\\\"|SenderType = \\\"Government\\\"" Services/ChatService.cs` confirms D-01 through D-06 are encoded in the service logic.
    `rg -n "UnauthorizedAccessException" Services/ChatService.cs` confirms live access rejection remains explicit for D-07.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1</automated>
  </verify>
  <done>The chat service owns one canonical unread rule for send, count, message projection, and mark-read behavior, and it encodes the locked Phase 1 applicant/reviewer/admin semantics directly.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify the controller unread endpoints and make the Phase 1 suite green</name>
  <read_first>
    .planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
    Controllers/ChatController.cs
    Services/Interfaces/IChatService.cs
    Services/ChatService.cs
    tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs
  </read_first>
  <files>Controllers/ChatController.cs, tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs</files>
  <action>Update `Controllers/ChatController.cs` so `MarkAsRead` and `GetUnreadCount` no longer compute `senderType` in the controller and instead call the new `IChatService` signatures directly. Preserve the explicit route names and the `Ok(new { UnreadCount = count })` response shape because later frontend work depends on that stable contract. If the test names or filters need small adjustments to match final method signatures, update `tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs` without weakening the `CHAT_01`, `CHAT_02`, or `CHAT_03` assertions. Finish by making the full Phase 1 test filter green.</action>
  <acceptance_criteria>
    `rg -n "MarkAsReadAsync\\(applicationId, userId, userRole\\)|GetUnreadCountAsync\\(applicationId, userId, userRole\\)" Controllers/ChatController.cs` shows the controller delegates sender-type decisions to the service.
    `rg -n "Ok\\(new \\{ UnreadCount = count \\}\\)" Controllers/ChatController.cs` confirms the unread-count response shape is unchanged.
    `dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1` reports all Phase 1 tests passing.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1</automated>
  </verify>
  <done>The chat controller stays thin, the unread-count contract remains explicit and stable, and the Phase 1 automated suite passes against the implemented unread semantics.</done>
</task>

</tasks>

<verification>
Run `dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1` and confirm `CHAT_01`, `CHAT_02`, and `CHAT_03` all pass with the updated service/controller contracts.
</verification>

<success_criteria>
Applicants, assigned reviewers, and admins now get actor-specific unread behavior through one chat-service rule path; explicit mark-read remains the clearing mechanism; and Phase 1 tests verify unauthorized and unrelated-scope behavior is blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-scoped-unread-rules/01-access-scoped-unread-rules-03-SUMMARY.md`
</output>
