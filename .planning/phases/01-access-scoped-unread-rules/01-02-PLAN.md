---
phase: 01-access-scoped-unread-rules
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Entities/Message.cs
  - Entities/MessageRecipientState.cs
  - Data/ApplicationDbContext.cs
  - Repositories/Interfaces/IMessageRepository.cs
  - Repositories/MessageRepository.cs
  - Repositories/Interfaces/IApplicationRepository.cs
  - Repositories/ApplicationRepository.cs
  - Migrations/<timestamp>_AddMessageRecipientStates.cs
  - Migrations/<timestamp>_AddMessageRecipientStates.Designer.cs
  - Migrations/ApplicationDbContextModelSnapshot.cs
autonomous: true
requirements:
  - CHAT-03
must_haves:
  truths:
    - "Unread state is stored per message recipient instead of only on the shared `Message.IsRead` flag."
    - "Recipient rows can distinguish two reviewers or admins on the same application and let one actor clear without clearing another."
    - "Repositories expose application-scope and recipient-scope unread primitives needed by the canonical chat rule."
  artifacts:
    - path: "Entities/MessageRecipientState.cs"
      provides: "Recipient-level unread persistence model"
      contains: "public class MessageRecipientState"
    - path: "Repositories/Interfaces/IMessageRepository.cs"
      provides: "Recipient-aware unread repository contract"
      contains: "Task<int> GetUnreadCountAsync(int applicationId, int recipientUserId, string senderType);"
    - path: "Repositories/ApplicationRepository.cs"
      provides: "Unread scope application loader with assignment graph"
      contains: "GetUnreadScopeApplicationAsync"
  key_links:
    - from: "Entities/Message.cs"
      to: "Entities/MessageRecipientState.cs"
      via: "navigation collection"
      pattern: "ICollection<MessageRecipientState>"
    - from: "Repositories/MessageRepository.cs"
      to: "Data/ApplicationDbContext.cs"
      via: "MessageRecipientStates DbSet queries"
      pattern: "MessageRecipientStates"
---

<objective>
Create the unread persistence contract that makes actor-specific counts and clears possible.

Purpose: Replace the current global unread mechanics with recipient-row primitives that later service logic can enforce safely.
Output: New unread recipient entity, EF model and migration, and repository interfaces/queries for recipient-scoped unread operations.
</objective>

<execution_context>
@C:/Users/Harvie/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Harvie/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
@.planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
@Data/ApplicationDbContext.cs
@Entities/Message.cs
@Repositories/Interfaces/IMessageRepository.cs
@Repositories/MessageRepository.cs
@Repositories/Interfaces/IApplicationRepository.cs
@Repositories/ApplicationRepository.cs
@Entities/ApplicationDepartmentReview.cs
@Migrations/20260124073649_AddChatTables.cs

<interfaces>
Current `Message` model:
```csharp
public class Message
{
    public int Id { get; set; }
    public int ApplicationId { get; set; }
    public int SenderId { get; set; }
    public string SenderType { get; set; } = string.Empty;
    public bool IsRead { get; set; } = false;
}
```

Current unread repository contract:
```csharp
Task<int> GetUnreadCountAsync(int applicationId, string senderType);
Task MarkAsReadAsync(int applicationId, string senderType);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the recipient-state entity, message navigation, and EF migration</name>
  <read_first>
    .planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
    .planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
    Entities/Message.cs
    Data/ApplicationDbContext.cs
    Migrations/20260124073649_AddChatTables.cs
  </read_first>
  <files>Entities/Message.cs, Entities/MessageRecipientState.cs, Data/ApplicationDbContext.cs, Migrations/&lt;timestamp&gt;_AddMessageRecipientStates.cs, Migrations/&lt;timestamp&gt;_AddMessageRecipientStates.Designer.cs, Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <action>Create `Entities/MessageRecipientState.cs` with the exact fields `Id`, `MessageId`, `RecipientUserId`, `RecipientRole`, `SenderType`, `IsRead`, `ReadAt`, and navigation properties to `Message` and `User`. Update `Entities/Message.cs` to add `ICollection<MessageRecipientState> RecipientStates { get; set; } = new List<MessageRecipientState>();` while keeping `IsRead` mapped for legacy compatibility but no longer treating it as the unread source of truth. Update `Data/ApplicationDbContext.cs` to register `DbSet<MessageRecipientState> MessageRecipientStates => Set<MessageRecipientState>();` and configure a required `SenderType`, `RecipientRole`, FK to `Messages`, FK to `Users`, and indexes on `(RecipientUserId, IsRead, SenderType)` and `(MessageId, RecipientUserId)` with a uniqueness constraint. Generate an EF migration named `AddMessageRecipientStates` so the table and indexes are committed as real migration files.</action>
  <acceptance_criteria>
    `rg -n "class MessageRecipientState|RecipientUserId|RecipientRole|ReadAt|SenderType" Entities/MessageRecipientState.cs` matches the new entity shape.
    `rg -n "ICollection<MessageRecipientState>|RecipientStates" Entities/Message.cs` confirms the message navigation exists.
    `rg -n "DbSet<MessageRecipientState> MessageRecipientStates|modelBuilder.Entity<MessageRecipientState>|HasIndex\\(e => new \\{ e.MessageId, e.RecipientUserId \\}\\)|IsUnique\\(" Data/ApplicationDbContext.cs` confirms the EF model wiring.
    `rg -n "CreateTable\\(|MessageRecipientStates|IX_MessageRecipientStates" Migrations` finds the generated migration artifacts.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build ePermitsApp.sln</automated>
  </verify>
  <done>The database model can persist multiple unread recipient rows per message and EF migration artifacts are present in source control.</done>
</task>

<task type="auto">
  <name>Task 2: Replace global unread repository methods with recipient-aware query primitives</name>
  <read_first>
    .planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
    .planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
    Repositories/Interfaces/IMessageRepository.cs
    Repositories/MessageRepository.cs
    Repositories/Interfaces/IApplicationRepository.cs
    Repositories/ApplicationRepository.cs
    Services/ApplicationService.cs
    Entities/ApplicationDepartmentReview.cs
    Data/ApplicationDbContext.cs
  </read_first>
  <files>Repositories/Interfaces/IMessageRepository.cs, Repositories/MessageRepository.cs, Repositories/Interfaces/IApplicationRepository.cs, Repositories/ApplicationRepository.cs</files>
  <action>Change `IMessageRepository` to expose the exact recipient-aware signatures `Task AddRecipientStatesAsync(IEnumerable<MessageRecipientState> recipientStates);`, `Task<int> GetUnreadCountAsync(int applicationId, int recipientUserId, string senderType);`, and `Task MarkAsReadAsync(int applicationId, int recipientUserId, string senderType);`. Update `MessageRepository` so unread count and mark-read operate on `MessageRecipientStates` joined back to `Messages` by `ApplicationId`, and mark-read sets only matching rows `IsRead = true` plus `ReadAt = DateTime.UtcNow`. Also add `Task<Application?> GetUnreadScopeApplicationAsync(int id);` to `IApplicationRepository` and implement it in `ApplicationRepository` to eagerly load `Application.User`, `Application.DepartmentReviews`, `DepartmentReviews.AssignedReviewer`, and assigned reviewers' `UserRole`/`Department` for canonical unread scope resolution in the next plan. Keep all EF queries inside repositories per repo convention, and do not introduce notification-specific controllers or services in this plan.</action>
  <acceptance_criteria>
    `rg -n "AddRecipientStatesAsync|GetUnreadCountAsync\\(int applicationId, int recipientUserId, string senderType\\)|MarkAsReadAsync\\(int applicationId, int recipientUserId, string senderType\\)" Repositories/Interfaces/IMessageRepository.cs Repositories/MessageRepository.cs` matches the new repository contract and implementation.
    `rg -n "ReadAt = DateTime\\.UtcNow|MessageRecipientStates|RecipientUserId" Repositories/MessageRepository.cs` confirms actor-specific mark-read logic updates recipient rows only.
    `rg -n "GetUnreadScopeApplicationAsync" Repositories/Interfaces/IApplicationRepository.cs Repositories/ApplicationRepository.cs` confirms the application-scope loader exists.
    `dotnet build ePermitsApp.sln` succeeds after the repository contract changes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet build ePermitsApp.sln</automated>
  </verify>
  <done>The repository layer now exposes only recipient-aware unread count, clear, and recipient-state persistence APIs, plus a dedicated unread-scope application loader for service-layer authorization.</done>
</task>

</tasks>

<verification>
Run `dotnet build ePermitsApp.sln` and verify the generated migration, new entity, and updated repository interfaces compile together.
</verification>

<success_criteria>
Unread state is stored in recipient rows, repository signatures no longer depend on the global `Message.IsRead` flag, and the service layer has a concrete application-loading seam for assignment-aware unread scope.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-scoped-unread-rules/01-access-scoped-unread-rules-02-SUMMARY.md`
</output>
