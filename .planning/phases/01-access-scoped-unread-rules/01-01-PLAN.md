---
phase: 01-access-scoped-unread-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ePermitsApp.sln
  - tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj
  - tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTestHost.cs
  - tests/ePermitsApp.Tests/Phase1/UnreadFixtureBuilder.cs
  - tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
must_haves:
  truths:
    - "Phase 1 has executable tests for applicant-owned unread scope, assigned-reviewer/admin scope, and actor-specific mark-read behavior."
    - "The execution phase can prove unauthorized and access-loss cases before unread code is changed."
    - "Phase 1 no longer relies on MISSING automated verification placeholders."
  artifacts:
    - path: "tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj"
      provides: "Backend test project wired into the solution"
      contains: "ProjectReference Include=\"..\\..\\ePermitsApp.csproj\""
    - path: "tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs"
      provides: "Requirement-traceable unread semantics tests"
      contains: "CHAT_01"
    - path: "tests/ePermitsApp.Tests/Phase1/UnreadFixtureBuilder.cs"
      provides: "Reusable seeded fixture for applicant, assigned reviewer, admin, and unassigned reviewer cases"
      contains: "AssignedReviewerId"
  key_links:
    - from: "tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs"
      to: "Services/ChatService.cs"
      via: "direct service integration tests"
      pattern: "GetUnreadCountAsync|MarkAsReadAsync|SendMessageAsync"
    - from: "tests/ePermitsApp.Tests/Phase1/UnreadFixtureBuilder.cs"
      to: "Entities/ApplicationDepartmentReview.cs"
      via: "seeded assignment graph"
      pattern: "AssignedReviewerId"
---

<objective>
Create the Wave 0 verification baseline for unread chat scope.

Purpose: Make Phase 1 executable with requirement-traceable automated tests before persistence and unread-rule code is changed.
Output: A backend test project, seeded Phase 1 fixtures, and failing tests for `CHAT-01`, `CHAT-02`, and `CHAT-03`.
</objective>

<execution_context>
@C:/Users/Harvie/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Harvie/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
@.planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
@.planning/phases/01-access-scoped-unread-rules/01-VALIDATION.md
@ePermitsApp.sln
@Services/ChatService.cs
@Services/Interfaces/IChatService.cs
@Repositories/MessageRepository.cs
@Repositories/Interfaces/IMessageRepository.cs
@Services/ApplicationService.cs
@Repositories/ApplicationRepository.cs
@Entities/Message.cs
@Entities/ApplicationDepartmentReview.cs
@Data/ApplicationDbContext.cs

<interfaces>
From `Services/Interfaces/IChatService.cs`:
```csharp
public interface IChatService
{
    Task<IEnumerable<MessageDto>> GetMessagesAsync(int applicationId, int userId, string userRole);
    Task<MessageDto> SendMessageAsync(SendMessageDto dto, int userId, string userRole);
    Task MarkAsReadAsync(int applicationId, int userId, string userRole, string senderType);
    Task<int> GetUnreadCountAsync(int applicationId, int userId, string userRole, string senderType);
}
```

From `Repositories/Interfaces/IMessageRepository.cs`:
```csharp
public interface IMessageRepository
{
    Task<IEnumerable<Message>> GetByApplicationIdAsync(int applicationId);
    Task<Message> AddAsync(Message message);
    Task<int> GetUnreadCountAsync(int applicationId, string senderType);
    Task MarkAsReadAsync(int applicationId, string senderType);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the backend test project and Phase 1 fixture host</name>
  <read_first>
    .planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
    .planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
    .planning/phases/01-access-scoped-unread-rules/01-VALIDATION.md
    ePermitsApp.sln
    Data/ApplicationDbContext.cs
    Services/ChatService.cs
    Repositories/MessageRepository.cs
    Repositories/ApplicationRepository.cs
    Entities/Message.cs
    Entities/ApplicationDepartmentReview.cs
  </read_first>
  <files>ePermitsApp.sln, tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj, tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTestHost.cs, tests/ePermitsApp.Tests/Phase1/UnreadFixtureBuilder.cs</files>
  <action>Create `tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj` as an xUnit test project that references `../../ePermitsApp.csproj` and adds the exact packages `Microsoft.NET.Test.Sdk`, `xunit`, `xunit.runner.visualstudio`, and `Microsoft.EntityFrameworkCore.Sqlite`. Add the project to `ePermitsApp.sln`. In `tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTestHost.cs`, build an in-memory SQLite-backed `ApplicationDbContext`, register real `ApplicationRepository`, `MessageRepository`, `UserRepository`, and `ChatService`, and expose helpers to create fresh scopes per test. In `tests/ePermitsApp.Tests/Phase1/UnreadFixtureBuilder.cs`, seed applicant, admin, assigned department user, unassigned department user, two applications, review-office assignments, and baseline messages/recipient rows so later tests can assert D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08 without ad hoc setup.</action>
  <acceptance_criteria>
    `rg -n "Microsoft\\.NET\\.Test\\.Sdk|xunit|Microsoft\\.EntityFrameworkCore\\.Sqlite|ProjectReference Include=\\\"..\\\\..\\\\ePermitsApp\\.csproj\\\"" tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj` finds all required package and project references.
    `dotnet sln ePermitsApp.sln list` includes `tests\\ePermitsApp.Tests\\ePermitsApp.Tests.csproj`.
    `rg -n "class ChatUnreadScopeTestHost|CreateScope\\(|UseSqlite|EnsureCreated" tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTestHost.cs` matches the host setup.
    `rg -n "class UnreadFixtureBuilder|AssignedReviewerId|ApplicationDepartmentReview|CreateApplicant|CreateAdmin|CreateDepartmentUser" tests/ePermitsApp.Tests/Phase1/UnreadFixtureBuilder.cs` matches the fixture builder.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1 --no-restore</automated>
  </verify>
  <done>The repo contains a runnable test project and reusable fixture host for Phase 1 unread-scope tests, even if the Phase 1 assertions are still failing pending implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing unread-scope tests for Phase 1 requirements</name>
  <read_first>
    .planning/phases/01-access-scoped-unread-rules/01-CONTEXT.md
    .planning/phases/01-access-scoped-unread-rules/01-RESEARCH.md
    .planning/phases/01-access-scoped-unread-rules/01-VALIDATION.md
    tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTestHost.cs
    tests/ePermitsApp.Tests/Phase1/UnreadFixtureBuilder.cs
    Services/ChatService.cs
    Services/Interfaces/IChatService.cs
    Controllers/ChatController.cs
  </read_first>
  <files>tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs</files>
  <behavior>
    - Test 1: `CHAT_01` proves an applicant only counts reviewer/admin unread rows on an application where `Application.UserId == applicant.Id`, and a different applicant is rejected.
    - Test 2: `CHAT_02` proves an assigned `user` counts applicant unread only when `ApplicationDepartmentReview.AssignedReviewerId == user.Id`, an unassigned department user gets zero/unauthorized access, and an `admin` can count reviewer-side unread for the same application per D-03.
    - Test 3: `CHAT_03` proves `MarkAsReadAsync` clears only the current actor's opposite-side rows for one application, leaves another reviewer/admin/applicant recipient row untouched, and blocks access after assignment loss per D-07.
  </behavior>
  <action>Add `tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs` with explicit xUnit tests whose method names and/or `[Trait]` values include `Phase1`, `CHAT_01`, `CHAT_02`, and `CHAT_03`. Use the real `ChatService` from the test host, not hand-rolled mocks. Encode the Phase 1 admin decision explicitly: applicant-sent unread is also countable by admin users with current oversight access per D-03, but each admin must keep independent read state per D-04. Leave the assertions red against current production code so the next plans have a real target.</action>
  <acceptance_criteria>
    `rg -n "CHAT_01|CHAT_02|CHAT_03|Phase1" tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs` finds all requirement markers.
    `rg -n "GetUnreadCountAsync|MarkAsReadAsync|SendMessageAsync" tests/ePermitsApp.Tests/Phase1/ChatUnreadScopeTests.cs` shows the tests exercise the real chat service methods.
    `dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1` runs and reports at least one failing Phase 1 test before implementation changes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1</automated>
  </verify>
  <done>Phase 1 has failing automated tests that pin the unread visibility, assignment, access-loss, admin visibility, and actor-specific mark-read semantics required by `CHAT-01`, `CHAT-02`, and `CHAT-03`.</done>
</task>

</tasks>

<verification>
Run `dotnet test tests/ePermitsApp.Tests/ePermitsApp.Tests.csproj --filter Phase1` and confirm the Phase 1 tests execute under the new test project with red results that map to `CHAT_01`, `CHAT_02`, and `CHAT_03`.
</verification>

<success_criteria>
The solution contains a committed backend test project, Phase 1 fixture infrastructure, and failing unread-scope tests that cover applicant ownership, assigned-reviewer access, admin visibility, actor-specific mark-read, unrelated-application isolation, and access-loss rejection.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-scoped-unread-rules/01-access-scoped-unread-rules-01-SUMMARY.md`
</output>
